public class CastingTest {
    public static void main(String[] args) {
        // Declaring.
        double x, y;
        int i, n;
        byte b;
        char c;

        // Initializing.
        x = 10.0;
        y = 3.0;
        i = (int) (x / y);
        n = (int) (x / y);

        System.out.println(i + ", " + (x / y));
    }
}
